# Database (local dev uses SQLite; swap to postgres:// for production)
DATABASE_URL="file:./dev.db"

# Auth
JWT_SECRET=change_this_to_a_long_random_string
ADMIN_EMAIL=admin@souqora.com
ADMIN_PASSWORD=change_this_password

# bKash manual payment (editable later from Admin Settings too)
BKASH_NUMBER=+8801872818201

# Only needed if you later connect a real bKash API gateway.
# Leaving these empty keeps bKash as MANUAL payment (as specified).
BKASH_API_KEY=
BKASH_API_SECRET=
